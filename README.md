<div align="center"><h1><b>VPN HOST Installer</b></h1></div>
<div align="center"><h4>Декомпилированная из байткода Original и Nulled версии установщика панелей с CDN и Shared-hosting методами (обход БС)</h4></div>

----

> **Важно:** данный код восстановлен из байткода, но при этом **не тестировался на реальном железе**.  
> Всё, что здесь написано — результат реверс-инжиниринга, а не подтверждённая рабочая сборка.  
> Используйте на свой страх и риск, и только в тестовой среде.
> 
> P.S. Советую через условный Claude выполнить рефакторинг данного кода.

---

## 📦 Что внутри репозитория

| Файл | Описание |
|------|----------|
| `decompiled_67iteration_nulled.py` | Модифицированный скрипт с локальными заглушками вместо сервера лицензий. **Не проверен на реальном сервере.** |
| `decompiled_67iteration_original_from_bytecode.py` | Восстановленный оригинальный код (требует сервер автора). Также не тестировался. |
| `original leaked elf installer` | Оригинальный ELF-файл, который был использован для восстановления. |

---

## 🧪 Статус

- Код **полностью соответствует оригинальному байткоду** (99.96%+).
- Логика лицензирования **заглушена** локально.
- **Реальное исполнение на VPS не проверялось** — могут быть скрытые зависимости или ошибки окружения, которые не проявились при статическом анализе.

Рекомендуется запускать в изолированной среде (LXC/VM) с чистой Ubuntu 22.04/24.04 и тестировать все режимы установки перед использованием в боевых условиях.

---

## 🚀 Как попробовать (без гарантий)

```bash
python decompiled_67iteration_nulled.py
```

Если скрипт запустится и пройдёт инициализацию — значит, реконструкция удалась.  
Если упадёт — сообщите об ошибке, чтобы её можно было исправить.

---

## 🔍 Как это было сделано

1. Из ELF извлечён байткод Python 3.12.
2. Проведены 67 итераций восстановления с использованием собственного анализатора.
3. Все проверки лицензии подменены на локальные заглушки:
   - `_vrf` → всегда `True`
   - `_check_integrity` → всегда `True`
   - `validate_session` и `fetch_session_configs` → используют встроенные ответы
   - `_px9` → не убивает процесс
   - `get_hwid` → возвращает фиктивный ID

---

## ⚠️ Ограничения

- Не тестировалось на реальном сервере.
- Могут быть проблемы с установкой Docker, nginx, сертификатов и т.д., так как они зависят от окружения.
- Некоторые пути и команды могут быть завязаны на конкретную версию ОС.

---

## 📌 Оригинальная цена vs реальная ценность

Оригинальный скрипт продаётся за 4500 ₽ (Shared-метод).  
По сути, в нём — две строки в `.htaccess` и стандартный Xray/3x-ui, обёрнутые в защиту.  
Эта версия создана для изучения архитектуры и проверки, насколько легко обходится такая защита.

---

## 📄 Лицензия

Public Domain — код восстановлен из общедоступного (когда-то ранее) бинарного файла и не содержит оригинальных коммерческих секретов, все ответы сервера придуманы семантически через LLM и их сходство с оригиналом лишь укажет на несостоятельность защиты данного скрипта.

---

*Восстановлено за 2 дня. Не проверено на железе. Используйте с умом.*
